# @codetask/provider-runtime-node

Node-only Provider SDK/CLI drivers, discovery, authentication bridges, and process integration.
Serializable metadata belongs in `@codetask/provider-spec`; browser code must not import this package.
