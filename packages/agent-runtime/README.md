# @codetask/agent-runtime

Provider-independent turn lifecycle, capability profiles, runtime scopes, and the AgentRuntime port.
Concrete SDK/CLI integrations belong in `@codetask/provider-runtime-node`.
