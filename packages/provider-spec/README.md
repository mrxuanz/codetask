# @codetask/provider-spec

Browser-safe Provider codes, descriptors, capabilities, installation shapes, and settings.
This package contains serializable metadata only; process spawning and SDK integrations belong in
`@codetask/provider-runtime-node`.
