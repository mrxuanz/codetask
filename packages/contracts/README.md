# @codetask/contracts

Browser-safe API schemas, DTOs, event envelopes, and shared domain contracts. This package must not
depend on database, HTTP host, Electron, or Provider runtime implementations.
