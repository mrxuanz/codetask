# Tombstone — Batch R3

Production contracts live in `@codetask/contracts` (`packages/contracts`).
Do not add new modules here. Importers under `src/server/**` and `apps/web/**`
must use `@codetask/contracts` only.
